{
  "name": "feedback-manager",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky"
  },
  "lint-staged": {
    "backend/**/*.ts": [
      "eslint --fix --config backend/eslint.config.js"
    ],
    "frontend/**/*.{ts,tsx}": [
      "eslint --fix --config frontend/eslint.config.js"
    ],
    "ai-services/**/*.py": [
      "bash -c 'cd ai-services && uv run ruff check --fix && uv run ruff format'"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  }
}
